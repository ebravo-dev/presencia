# En producción se recomienda mismo origen: nginx envía todas las rutas al gateway.
# Sólo se necesita esta variable si Vite corre separado durante desarrollo.
VITE_SUPER_USER_API_BASE_URL=http://localhost:8080/api
