import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — app requires Hive init so widget tests
    // need proper setup. This just validates the test runner works.
    expect(1 + 1, equals(2));
  });
}
